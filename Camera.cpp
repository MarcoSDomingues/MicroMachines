#include "Camera.h"

Camera::~Camera()
{
}

void Camera::setAt(float x, float y, float z) {
	/*if (_at == NULL) {
		_at = new Vector3(x, y, z);
	}
	else {
		_at->set(x, y, z);
	}*/
}

void Camera::update(float ratio) {}